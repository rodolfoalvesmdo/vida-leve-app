<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.myapplication3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/vida_leve_icon1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <activity android:name=".mainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".receitaDoisActivity" />
        <activity android:name=".receitaUmActivity"/>
        <activity android:name=".receitaTresActivity"/>
        <activity android:name=".receitaQuatroActivity"/>
        <activity android:name=".receitaCincoActivity"/>

        <activity
            android:name=".splashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>



</manifest>
